services:
  - type: web
    name: market-research-agent
    runtime: docker
    rootDir: day58_deploy_render
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ENV
        value: production
    healthCheckPath: /health
    plan: free